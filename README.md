```
python3 -m venv venv
source venv/bin/activate
python3 -m pip install opensearch-py python-dotenv requests
python3 -m pip install -r requirements.txt
cd src
python3 main.py export-ondemand-report
```
pyenv shell 3.11.6
to install
python3 -m pip install -r requirements.txt

to export 
python